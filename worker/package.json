{
  "name": "claude-rpc-totals-worker",
  "version": "0.1.0",
  "private": true,
  "description": "Cloudflare Worker that holds opt-in community totals for the claude-rpc README. Source for the badges shown on github.com/rar-file/claude-rpc.",
  "type": "module",
  "scripts": {
    "test": "node --test test/*.test.js",
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "tail": "wrangler tail",
    "kv:create": "wrangler kv:namespace create TOTALS",
    "kv:create:preview": "wrangler kv:namespace create TOTALS --preview"
  },
  "devDependencies": {
    "wrangler": "^4.94.0"
  }
}
